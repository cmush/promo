language: elixir

elixir: '1.9.0'

branches:
  only:
    - master

notifications:
  email: collinsmucheru@gmail.com

services:
  - docker

before_script:
  - travis/before_script.sh --travis

script:
  - travis/script.sh

after_script:
  - travis/after_script.sh